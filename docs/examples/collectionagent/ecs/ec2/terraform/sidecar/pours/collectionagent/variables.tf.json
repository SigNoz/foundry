{
  "variable": {
    "execution_role_name": {
      "nullable": false,
      "validation": {
        "condition": "${length(trimspace(var.execution_role_name)) > 0}",
        "error_message": "execution_role_name must name an IAM role."
      },
      "description": "Name of the IAM task execution role of the task definition the collector joins; the module attaches the parameter read policy to it",
      "type": "string"
    }
  }
}
