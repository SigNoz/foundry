[Unit]
Description=ClickHouse Server (v25.5.6)
Requires=network-online.target
After=time-sync.target network-online.target
Wants=time-sync.target


[Service]
Type=simple
User=clickhouse
Group=clickhouse
Restart=always
RestartSec=30
RuntimeDirectory=clickhouse
{{ if and $.Spec.TelemetryStore.Status.Extras (index $.Spec.TelemetryStore.Status.Extras "cfgPath") }}
ExecStart=/usr/bin/clickhouse-server --config=/etc/clickhouse-server/{{ index $.Spec.TelemetryStore.Status.Extras "cfgPath" }} --pid-file=/run/clickhouse/clickhouse.pid
{{ end }}
LimitCORE=infinity
LimitNOFILE=500000
CapabilityBoundingSet=CAP_NET_ADMIN CAP_IPC_LOCK CAP_SYS_NICE CAP_NET_BIND_SERVICE
	
[Install]
WantedBy=multi-user.target
