# The Kubernetes additions to the agent's collector config are not decided
# yet: which receivers scrape a node and its pods, and which processors
# attribute them, land here. An empty enricher config leaves the molding's
# base config untouched.
{}
