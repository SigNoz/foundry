apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - service.yaml
  - workload.yaml

configMapGenerator:
  - name: {{ $.Metadata.Name }}-collector-deployment
    files:
      - deployment.yaml
