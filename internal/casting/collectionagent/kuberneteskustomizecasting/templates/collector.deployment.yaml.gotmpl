# The Kubernetes additions to the environment-scoped collector's config are
# not decided yet: which receivers collect the once-per-cluster signals, and
# which processors attribute them, land here. An empty enricher config leaves
# the molding's base config untouched.
{}
